[
	{
		"type": "function",
		"function": {
			"name": "agents.List",
			"description": "List all currently working contractor agents.",
			"parameters": {
				"type": "object",
				"properties": {},
				"required": []
			}
		}
	},
	{
		"type": "function",
		"function": {
			"name": "agents.Hire",
			"description": "Hire a contractor agent with a unique name and a task prompt.",
			"parameters": {
				"type": "object",
				"properties": {
					"name": {
						"type": "string",
						"description": "Contractor agent's unique pseudonym name"
					},
					"agent": {
						"type": "string",
						"description": "Contractor agent's type",
						"enum": ["architect", "coder", "researcher", "tester"]
					},
					"sandbox": {
						"type": "string",
						"description": "Relative path to the folder"
					},
					"prompt": {
						"type": "string",
						"description": "Specific task instructions, agent will quit with a report after completion"
					}
				},
				"required": ["name", "agent", "sandbox", "prompt"]
			}
		}
	},
	{
		"type": "function",
		"function": {
			"name": "agents.Fire",
			"description": "Fire a contractor agent with a unique name.",
			"parameters": {
				"type": "object",
				"properties": {
					"name": {
						"type": "string",
						"description": "Contractor agent's unique name"
					}
				},
				"required": ["name"]
			}
		}
	},
	{
		"type": "function",
		"function": {
			"name": "agents.Inquire",
			"description": "Inquire a status report of the contractor agent with a unique name.",
			"parameters": {
				"type": "object",
				"properties": {
					"name": {
						"type": "string",
						"description": "Contractor agent's unique name"
					}
				},
				"required": ["name"]
			}
		}
	},
	{
		"type": "function",
		"function": {
			"name": "agents.Quit",
			"description": "Finish your work with a status report to the hiring agent.",
			"parameters": {
				"type": "object",
				"properties": {
					"message": {
						"type": "string",
						"description": "The status message. If it contains \"my work is done\" the current process exits with a success."
					}
				},
				"required": ["message"]
			}
		}
	}
]
